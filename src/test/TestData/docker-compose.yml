version: '3.7'

services:
  wordpress:
    image: wordpress
    ports:
      - 8082:80
    environment:
      WORDPRESS_DB_PASSWORD: son

  mysql:
    image: mysql:5.6
    environment:
      MYSQL_ROOT_PASSWORD: son